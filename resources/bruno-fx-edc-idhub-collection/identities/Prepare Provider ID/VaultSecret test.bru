meta {
  name: VaultSecret test
  type: http
  seq: 5
}

get {
  url: {{VAULTURL}}/v1/{{VAULT_PATH}}/data/{{VAULT_PROVIDER_SECRET_PATH}}
  body: none
  auth: inherit
}

headers {
  X-Vault-Token: {{VAULT_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
